<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.newgxu.dao.UserMapper">
	
	<select id="getUserByUN" resultType="cn.newgxu.model.UserModel">
	  select userName,password as passWord,nick from user where userName = #{userName}
	</select>
	
</mapper>